# ai-image-db
A tool to document within an SQLite database images and associated metadata, including watermarks and C2PA- can be extended with custom schemes; I did the design + architecture + tests, using Grok to build the different iterations; the tool can read recursively, validate watermarks, and produces a CSV listing images that could not process
