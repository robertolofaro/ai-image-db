# Core
Pillow>=10.0.0
piexif>=1.1.3
exifread>=3.0.0

# Database
# (stdlib sqlite3)

# C2PA
c2pa-python>=0.30.0

# Captioning / Tagging
torch>=2.0.0
torchvision
transformers>=4.40.0
timm
einops
# flash_attn optional / platform dependent
huggingface_hub
onnxruntime  # or onnxruntime-gpu
# WD14 often uses keras / onnx; we use a lightweight onnx path when available

# Utilities
tqdm
click
python-dateutil
numpy
pandas

# Optional but recommended for richer EXIF
pyexiftool  (requires system exiftool binary)
